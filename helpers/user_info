import os
import base64
import requests
import urllib.parse
from client import SpotifyClient

class UserInfo(SpotifyClient):
    def __init__(self, client_id, client_secret, redirect_uri) -> None:
        super().__init__(client_id, client_secret, redirect_uri)
        # Additional attributes specific to UserInfo class

    # Additional methods specific to UserInfo class

    def get_user_playlists(self):
        pass

    def get_user_top_artists(self):
        pass

    def get_user_top_tracks(self):
        pass

    def create_playlist(self, name, description, public=False):
        pass

    def add_tracks_to_playlist(self, playlist_id, track_uris):
        pass

    # Other methods as needed for the music recommendation app

class UserRecs(SpotifyClient):
    def __init__(self, client_id, client_secret, redirect_uri) -> None:
        super().__init__(client_id, client_secret, redirect_uri)
        # Additional attributes specific to UserRecs class